# package
OCAMLPACKS[] =
	extlib
	str
	unix

# Build an OCaml program
FILES[] =
	rule
	cmdOpt
	color
	$(ROOT)/config
PROGRAM = habc

OCamlProgram($(PROGRAM), main $(FILES))
OCamlLibrary($(PROGRAM), $(FILES))

# phony target
.PHONY: clean

.DEFAULT: $(PROGRAM)$(EXE)
clean:
	ocaml-clean $(PROGRAM)
