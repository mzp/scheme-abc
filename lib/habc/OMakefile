.PHONY: lib clean

FILES[] =
	flash
	std

LIBS=$(addsuffix .ho,$(FILES))
.DEFAULT: $(LIBS)

lib: $(LIBS)

%.ho : %.scm $(ROOT)/habc
	$(ROOT)/habc --scm=$(ROOT)/habc-scm -I $(ROOT)/lib/habc -c $<

clean:
	ocaml-clean

flash.ho : std.ho