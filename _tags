<src/*.ml> and not "src/debug.ml":pp(camlp4o -parser OCaml -filter Camlp4ExceptionTracer -printer Auto)
<src> or <test>: include

# for TestCase
"test/TestCaseCollector.ml": use_camlp4, camlp4of
<test/*.ml> and not "test/TestCaseCollector.ml":pp(camlp4o -I test TestCaseCollector.cmo)